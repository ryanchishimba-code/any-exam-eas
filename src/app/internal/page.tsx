import { PortalHomeDashboard } from "@/components/internal/PortalHomeDashboard";

export const metadata = {
  title: "Employee Portal — Any Exam Easy",
};

export default function InternalHomePage() {
  return <PortalHomeDashboard />;
}
