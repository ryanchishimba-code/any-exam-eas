/** NGN / advanced item guidance — detailed schema lives in high-yield.ts */
export const NGN_JSON_SCHEMA = `
Use NGN formats per HIGH-YIELD BOARD EXAM REQUIREMENTS (unfolding_case, bow_tie, select_all, matrix, highlight, ordered_response).
Each item MUST include vignette (when clinical), explanation, clinicalReasoning, distractorRationale, references, bloomLevel, and ngnFormat when applicable.`;

export const NGN_SYSTEM_AUGMENTATION = `You write high-yield board-style items superior to commercial banks.
Study the pattern analysis and retrieved chunks — mirror distractor logic and clinical judgment flow from exemplars.
Every item must be defensible from provided OER sources with per-distractor rationales.
Never copy stems verbatim; create fresh variations with equal or higher realism.
Self-check: vignette has discriminating data; distractors are plausible exam traps; rationale explains why each wrong option fails.`;
