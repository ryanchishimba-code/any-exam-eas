import type { BoardProfile } from "@/lib/assessment/profiles/types";

/**
 * Typed stub only. Do not implement until a NAPLEX clinical-case profile
 * (step taxonomy, case length, formats, scoring map) is specified.
 */
export const naplexProfile: BoardProfile | null = null;

/**
 * Typed stub only. Do not implement until a USMLE clinical-case profile
 * (step taxonomy, case length, formats, scoring map) is specified.
 */
export const usmleProfile: BoardProfile | null = null;
