# Drop this book into Cursor

## Fastest path (paste)
1. Open Cursor in your AnyExamEasy repo (or a new folder).
2. Copy `PASTE-READY-ANYEXAMEASY-FNP-BOOK.md` into the project root (or `content/fnp/`).
3. Visuals are **ALREADY in this folder** (`visuals/`). Do **not** hunt for a separate pack — keep `visuals/` beside the markdown so relative paths like `visuals/...` resolve.
4. Ask Cursor: "Wire this branded AANP FNP book into the site using our AnyExamEasy teal/navy brand tokens in the frontmatter."

## Zero-setup reading
Open `ANYEXAMEASY-FNP-BOOK-EMBEDDED.html` in any browser — images are base64-embedded (offline, no separate visuals folder needed).

## Files
| File | Use |
| --- | --- |
| `ANYEXAMEASY-FNP-BOOK-EMBEDDED.html` | **Self-contained** HTML reader (images inside) |
| `PASTE-READY-ANYEXAMEASY-FNP-BOOK.md` | Paste-ready full book + brand tokens |
| `BOOK.md` | Master assembly (operator version) |
| `00`–`16` chapter `.md` files | Edit by chapter |
| `visuals/` | Cover + all diagrams (**included**, compressed JPG) |
| `embedded/` | Split HTML chapters if the single file is large |

## Brand
Accent teal `#0D9488` · Navy `#071A2C` · Cyan `#00E5D8`
See frontmatter in the paste-ready file.
