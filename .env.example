# App
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-long-random-string-here

# Database — PostgreSQL only (SQLite file:./dev.db is not supported)
# Local: run ./scripts/dev.sh (Docker Postgres) OR paste a Neon URL below
# Vercel: add DATABASE_URL in Project → Environment Variables
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/anyexameasy"

# OpenAI — exam & learning content generation
OPENAI_API_KEY=

# Web search — Tavily recommended (https://tavily.com)
TAVILY_API_KEY=

# Stripe — $9/mo subscription with 7-day trial
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_ID=
